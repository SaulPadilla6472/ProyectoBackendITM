// Este es un código sencillo de que calcula el perimetro de un cuadrado 
function perimetroCuadrado(lado) {
    return lado * 4;
}

// Ejemplo de uso
var lado = 5;
var perimetro = perimetroCuadrado(lado);
console.log("El perímetro del cuadrado con lado", lado, "es", perimetro);
